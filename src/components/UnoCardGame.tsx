export default function UnoCardGamePage() {
    //players = []
    //int whosTurnIsIt
    //socket on ("player action", (res) =>  if res != my player -> player(res.playerid) place card (orange 4) & next player is sataar)     // res = {actin: "Julian Orange 4"}
    //if sataar = my username => socket emit ("gamechange", { lobbyid, action:"sataar blue 5", nextplayer:"erdem"})
    return (<>
    </>);
}